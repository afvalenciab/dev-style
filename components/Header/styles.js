import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  appBar: {
    boxShadow: 'none',
    marginBottom: '2.5rem',
  },
  container: {
    height: '60px',
  },
});
